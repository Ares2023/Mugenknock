# カナリア整合性チェック (20260704_054505)

## 所見
現行のapp/ルートとカナリアspecはほぼ整合しており、陳腐化（存在しない/改名ルートを指すテスト）は無い。ユーザー向け公開ページ（/, /about, /encyclopedia, /privacy-policy, /exam-guide[/type], /services[/name], /questions/[type][/id], /login）は全てカバー済み。唯一、公開ながら未カバーの `/admin-login`（未ログインで到達可・Amplify Authenticatorのemail入力を描画、リダイレクトしない）だけスモークが欠けていたため、既存の『ページ表示チェック』様式で1行追記した。

## カバー漏れ・陳腐化
- カバー漏れ: /admin-login が未ログインで到達可能な公開ルート（AdminLogin が Amplify Authenticator を描画、認証前でもリダイレクトせず表示される）にも関わらず、canary.noauth.spec.ts のページ表示チェックに含まれていない。→ 既存様式で input[type="email"] の表示確認を追記。
- 陳腐化: 検出なし。テスト対象パス（/services/ec2, /exam-guide/SAA, /questions/SAA 等）は全て app/ に実在し、改名・削除されたルートを指すテストは無い。
- 対象外の確認: /aws/*・/account・/admin は認証必須のため no-auth カナリア対象外として据え置き（テスト追加せず）。

## 対応
- specを更新（検証OK=playwright --list 成功）。
- バックアップ: /home/yuzuki/aws-quiz-app/prompts/night-prompts/logs/canary-coverage_20260704_054505_spec.bak

```diff
--- a/e2e/tests/canary.noauth.spec.ts
+++ b/e2e/tests/canary.noauth.spec.ts
@@ -62,6 +62,7 @@
   const pages = [
     { path: '/',                label: 'ランディングページ',      check: 'header, main, h1, h2' },
     { path: '/login',           label: 'ログインページ',          check: 'input[type="email"]' },
+    { path: '/admin-login',     label: '管理者ログインページ',    check: 'input[type="email"]' },
     { path: '/about',           label: 'Aboutページ',             check: 'text=プライバシーポリシー' },
     { path: '/encyclopedia',    label: 'サービス図鑑（公開）',    check: 'main' },
     { path: '/privacy-policy',  label: 'プライバシーポリシー',    check: 'h1' },
```

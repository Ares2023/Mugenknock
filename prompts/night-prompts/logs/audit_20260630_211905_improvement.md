# プロンプト改良レポート (20260630_211905)

## 改良方針
監査の主クラスは『易しすぎ（単一キーワードで正解が一意に想起／不正解選択肢が弱く消去法で容易）』と『要確認（旧名称サービス）』。生成側(_common-rules)に弁別性・ひっかけ選択肢の必須化を追記し、検証側(_validity-extra)に単一キーワード問題と旧名称の warn 観点を注入する。SAA.txt は scope=out が0のため変更なし。

## 適用 2件 / 見送り 0件

### ✅ _common-rules.txt
**理由:** too_easy 2件（「マイクロ秒」→DAX、「マルチAZ共有ストレージ」→EFS が一読で自明／不正解選択肢が明らかに弱く消去法で容易）に対応。生成時点で単一キーワード一意特定を避け、近接した実在の代替案を不正解に置くことを必須化する。

```diff
--- a/_common-rules.txt
+++ b/_common-rules.txt
@@ -30,6 +30,11 @@
 - 正解だけが著しく長い・短いと文字数から正解が推測できてしまう
 - 正解の文字数を不正解の平均文字数に近づけ、見た目で正解が特定できないようにすること
 
+■ 難易度・選択肢の弁別性（易しすぎ防止）
+- 単一のキーワードが正解サービスを一意に想起させる出題を避ける（例:「マイクロ秒」→DAX、「マルチAZ共有ストレージ」→EFS）。複数の要件・制約・トレードオフを組み合わせ、キーワード一致だけでは絞れないようにする
+- 不正解選択肢に「明らかに弱い／無関係／実在しない」案を置かないこと。各選択肢は一見もっともらしく、要件の細部で初めて脱落する近接した実在の代替案にする（例: EFS の対抗に EBS Multi-Attach・FSx など）
+- 定番パターン（Firehose の Parquet 変換等）に頼る場合も、要件の細部で正解が決まるよう問題文に判別条件を埋め込み、消去法だけで解けないようにする
+
 ■ 選択肢説明のフォーマット
 - 「選択肢Aの[サービス名]は〜」形式（サービス名は略さない）
 - 各選択肢説明は改行で区切る（連続した段落にしない）
```

### ✅ _validity-extra.txt
**理由:** 検証ゲートで弾く観点を追加。too_easy（単一キーワードで一意特定／弱い不正解選択肢）を warn、uncertain（旧名称 Kinesis Data Firehose→Amazon Data Firehose 等）を名称現行性の warn として注入する。

```diff
--- a/_validity-extra.txt
+++ b/_validity-extra.txt
@@ -1,3 +1,10 @@
 # 監査(audit-questions.sh -i)が継続的に追記する「追加の確認観点」。
 # 02-check-validity.sh がこのファイルの内容を検証プロンプトの確認観点へ自動注入する。
 # 空でも可。手動編集も可だが、自動改良で上書きされることがある。
+
+## 易しすぎ・弁別性（warn）
+- 問題文中の単一キーワードが正解サービスを一意に特定できる場合（例:「マイクロ秒」→DAX、「マルチAZ共有ストレージ」→EFS）は warn。本番相当には複数要件・制約の組み合わせが必要。
+- 不正解選択肢が明らかに無関係・実在しない・弱く、消去法で容易に絞れる場合は warn（近接した実在の代替案になっているか確認）。
+
+## 現行性・名称（warn）
+- 旧名称のサービスは現行名称か確認する（例: Kinesis Data Firehose → Amazon Data Firehose〔2024年改称〕）。機能・正解が妥当でも名称が旧称なら warn。
```


バックアップ: /home/yuzuki/aws-quiz-app/prompts/night-prompts/logs/audit_20260630_211905_promptbackup

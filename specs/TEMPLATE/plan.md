# <機能名> — 設計

`spec.md` の受入条件を満たすための実装方針。**書く前に `docs/` の該当章を読むこと。**

## 参照した既存仕様

<!-- 読んだ docs のファイルと節。読んでいないなら書かない（嘘の根拠を作らない）。 -->

- `docs/0X-....md` §X.X —

## アプローチ

<!-- 選んだ方法と、選ばなかった方法をなぜ却下したか。後から読む人のために。 -->

### 採用案

### 却下した案

| 案 | 却下理由 |
|---|---|

## 変更するファイル

| ファイル | 変更内容 |
|---|---|
| | |

## データモデルの変更

<!-- 無ければ「なし」。 -->

| テーブル | 属性 | 型 | 既存データの扱い |
|---|---|---|---|

**既存データの移行が必要か**: はい / いいえ
（必要なら移行スクリプトもこの plan に含める。DynamoDB は dev/prod でコンテンツ系が共有である点に注意）

## API の変更

<!-- 無ければ「なし」。 -->

| メソッド | パス | 認証 | リクエスト | レスポンス |
|---|---|---|---|---|

**後方互換**: 既存クライアント（デプロイ済みフロント）が壊れないか確認する。
静的サイトなのでユーザーのブラウザには**古いJSがキャッシュされている**前提で設計する。

## 状態・永続化

<!-- localStorage / sessionStorage を増やす場合のみ。 -->

| キー | 内容 | kvSync 同期 | ゲストでも使うか |
|---|---|---|---|

> **累積して増えるデータをゲストの localStorage に置かない**（`CLAUDE.md` の原則）。
> kvSync に載せる場合、配列を蓄積する「まるごとブロブ」は LWW で壊れるため対象外にする。

## テスト方針

- [ ] ユニットテスト（対象:  ）
- [ ] E2E で確認（対象:  ）
- [ ] 手動確認の手順:

## デプロイ手順

- [ ] `git push github develop`
- [ ] `./scripts/deploy-lambda.sh dev`（Lambda 変更時）
- [ ] `./prompts/night-prompts/scripts/cf-deploy-status.sh wait`
- [ ] 検証環境で受入条件を確認
- [ ] （ユーザーの明示指示後）`master` マージ + `./scripts/deploy-lambda.sh prod`

> **`git push` では Lambda は更新されない。** Lambda を変えたら dev/prod 両方に出す。

## リスクと巻き戻し方

| リスク | 影響 | 巻き戻し方 |
|---|---|---|
| | | |

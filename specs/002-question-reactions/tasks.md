# 問題ごとの👍👎リアクション ＆ 演習画面フッターの整理 — 実装タスク

上から順に実装する。1タスク = 1コミットを目安にする。

## 実装

- [x] 1. Lambda: `PUT /questions/:id/reaction` を追加、`question-status` に `reactions` を追加
- [x] 2. Lambda: 管理画面の問題一覧に `reactionUp`/`reactionDown` を含める
- [x] 3. `PromptMenu`（質問プロンプト生成）をコンポーごと削除
- [x] 4. 見出し右の☆（ブックマーク）を削除し、♡として解説下アクション列へ移動
- [x] 5. `renderActionRow()` で `[コピー][♡][👍][👎][⋮]` を左揃えで実装。
      回答前（選択肢の下）と回答後（解説の下）で同じ並びになるようにする
- [x] 6. フッターの通報・途中採点ボタンを削除し、⋮メニューへ格納
- [x] 7. 管理画面の問題一覧に 👍/👎 集計を表示

## テスト

- [x] 手動で受入条件（spec.md）を確認
      （プロンプト生成が無いこと／アクション列の並び／👍のサーバ保存とリロード復元／
      ⋮メニューの通報・途中採点動作／管理画面の集計表示）

## デプロイ

- [x] `./scripts/deploy-lambda.sh dev`
- [ ] develop へ push
- [ ] Cloudflare Pages のビルド完了を確認
- [ ] 検証環境で受入条件をすべて確認

## クローズ

- [ ] `docs/05-screens.md`（ExerciseSession節）・`docs/04-api.md` を更新
- [ ] `spec.md` の状態を「完了（YYYY-MM-DD）」に更新
- [ ] 想定と違ったこと・学んだことを下に記録

## 実装後のメモ

- 演習画面のフッターにあった「通報」「途中採点」ボタンは `AlertTriangle`/`Check` アイコンを
  使っていたが、⋮メニューに移したことでこれらのアイコンは演習画面から使われなくなった
  （import からは削除済み。Icons.tsx 自体からの削除は他画面での使用有無を確認していないため
  今回は見送った）。
